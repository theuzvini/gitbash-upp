<h1>Lista de Dados</h1>
<div class="dadoscd">
    <a href="/">Adicionar</a>
    {{#each teste}}
        <div>#{{this.id}}- <span class="bold">{{this.nome}} {{this.email}}</span></div>
    {{/each}}
</div>